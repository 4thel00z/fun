---
title: Upgrade Fun to the latest version
sidebarTitle: Upgrade Fun
mode: center
---

Fun can upgrade itself using the built-in `fun upgrade` command. This is the fastest way to get the latest features and bug fixes.

```bash terminal icon="terminal"
fun upgrade
```

This downloads and installs the latest stable version of Fun, replacing the currently installed version.

<Note>To see the current version of Fun, run `fun --version`.</Note>

---

## Verify the upgrade

After upgrading, verify the new version:

```bash terminal icon="terminal"
fun --version
# Output: 1.x.y

# See the exact commit of the Fun binary
fun --revision
# Output: 1.x.y+abc123def
```

---

## Upgrade to canary builds

Canary builds are automatically released on every commit to the `main` branch. These are untested but useful for trying new features or verifying bug fixes before they're released.

```bash terminal icon="terminal"
fun upgrade --canary
```

<Warning>Canary builds are not recommended for production use. They may contain bugs or breaking changes.</Warning>

---

## Switch back to stable

If you're on a canary build and want to return to the latest stable release:

```bash terminal icon="terminal"
fun upgrade --stable
```

---

## Install a specific version

To install a specific version of Fun, use the install script with a version tag:

<Tabs>
  <Tab title="macOS & Linux">
    ```bash terminal icon="terminal"
    curl -fsSL https://fun.dev/install | bash -s "fun-v1.3.3"
    ```
  </Tab>
  <Tab title="Windows">
    ```powershell PowerShell icon="windows"
    iex "& {$(irm https://fun.dev/install.ps1)} -Version 1.3.3"
    ```
  </Tab>
</Tabs>

---

## Package manager users

If you installed Fun via a package manager, use that package manager to upgrade instead of `fun upgrade` to avoid conflicts.

<Tip>
**Homebrew users** <br />
To avoid conflicts with Homebrew, use `brew upgrade fun` instead.

**Scoop users** <br />
To avoid conflicts with Scoop, use `scoop update fun` instead.

</Tip>

---

## See also

- [Installation](/installation) — Install Fun for the first time
- [Update packages](/pm/cli/update) — Update dependencies to latest versions
