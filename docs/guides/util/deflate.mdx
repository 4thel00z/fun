---
title: Compress and decompress data with DEFLATE
sidebarTitle: DEFLATE compression
mode: center
---

Use `Fun.deflateSync()` to compress a `Uint8Array` with DEFLATE.

```ts
const data = Buffer.from("Hello, world!");
const compressed = Fun.deflateSync("Hello, world!");
// => Uint8Array

const decompressed = Fun.inflateSync(compressed);
// => Uint8Array
```

---

See [Docs > API > Utils](/runtime/utils) for more useful utilities.
