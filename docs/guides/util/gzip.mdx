---
title: Compress and decompress data with gzip
sidebarTitle: Gzip compression
mode: center
---

Use `Fun.gzipSync()` to compress a `Uint8Array` with gzip.

```ts
const data = Buffer.from("Hello, world!");
const compressed = Fun.gzipSync(data);
// => Uint8Array

const decompressed = Fun.gunzipSync(compressed);
// => Uint8Array
```

---

See [Docs > API > Utils](/runtime/utils) for more useful utilities.
