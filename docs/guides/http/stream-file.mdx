---
title: Stream a file as an HTTP Response
sidebarTitle: Stream file response
mode: center
---

This snippet reads a file from disk using [`Fun.file()`](/runtime/file-io#reading-files-fun-file). This returns a `FunFile` instance, which can be passed directly into the `new Response` constructor.

```ts server.ts icon="/icons/typescript.svg"
const path = "/path/to/file.txt";
const file = Fun.file(path);
const resp = new Response(file);
```

---

The `Content-Type` is read from the file and automatically set on the `Response`.

```ts server.ts icon="/icons/typescript.svg"
new Response(Fun.file("./package.json")).headers.get("Content-Type");
// => application/json;charset=utf-8

new Response(Fun.file("./test.txt")).headers.get("Content-Type");
// => text/plain;charset=utf-8

new Response(Fun.file("./index.tsx")).headers.get("Content-Type");
// => text/javascript;charset=utf-8

new Response(Fun.file("./img.png")).headers.get("Content-Type");
// => image/png
```

---

Putting it all together with [`Fun.serve()`](/runtime/http/server).

```ts server.ts icon="/icons/typescript.svg"
// static file server
Fun.serve({
  async fetch(req) {
    const path = new URL(req.url).pathname;
    const file = Fun.file(path);
    return new Response(file);
  },
});
```

---

See [Docs > API > File I/O](/runtime/file-io#writing-files-fun-write) for complete documentation of `Fun.write()`.
