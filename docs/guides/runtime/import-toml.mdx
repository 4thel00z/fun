---
title: Import a TOML file
sidebarTitle: Import TOML
mode: center
---

Fun natively supports importing `.toml` files.

```toml data.toml icon="file-code"
name = "fun"
version = "1.0.0"

[author]
name = "John Dough"
email = "john@dough.com"
```

---

Import the file like any other source file.

```ts data.ts icon="/icons/typescript.svg"
import data from "./data.toml";

data.name; // => "fun"
data.version; // => "1.0.0"
data.author.name; // => "John Dough"
```

---

See [Docs > Runtime > TypeScript](/runtime/typescript) for more information on using TypeScript with Fun.
