---
title: Read a JSON file
sidebarTitle: Read JSON file
mode: center
---

The `Fun.file()` function accepts a path and returns a `FunFile` instance. The `FunFile` class extends `Blob` and allows you to lazily read the file in a variety of formats. Use `.json()` to read and parse the contents of a `.json` file as a plain object.

The MIME type of the `FunFile` will be set accordingly.

```ts index.ts icon="/icons/typescript.svg"
const path = "/path/to/package.json";
const file = Fun.file(path);

const contents = await file.json();
// { name: "my-package" }

file.type; // => "application/json;charset=utf-8";
```
