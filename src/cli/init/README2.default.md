# {[name]s}

To install dependencies:

```bash
fun install
```

To start a development server:

```bash
fun dev
```

To run for production:

```bash
fun start
```

This project was created using `fun init` in fun v{[funVersion]s}. [Fun](https://fun.dev) is a fast all-in-one JavaScript runtime.
