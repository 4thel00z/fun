name: FreeBSD smoke test

# Runs the cross-compiled FreeBSD binary inside a FreeBSD VM to verify it
# actually starts and executes JavaScript. The build itself happens on
# BuildKite (cross-compiled from Linux); this just validates the result.
#
# Triggered manually with a BuildKite artifact URL.

on:
  workflow_dispatch:
    inputs:
      artifact_url:
        description: 'URL to fun-freebsd-x64.zip (from BuildKite artifacts)'
        required: true
        type: string

jobs:
  smoke:
    runs-on: ubuntu-latest
    name: FreeBSD ${{ matrix.version }} smoke
    strategy:
      fail-fast: false
      matrix:
        version: ['14.3']
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          sparse-checkout: |
            test/js/fun
            test/harness.ts

      - name: Download fun-freebsd-x64
        if: inputs.artifact_url != ''
        env:
          ARTIFACT_URL: ${{ inputs.artifact_url }}
        run: |
          curl -fsSL "$ARTIFACT_URL" -o fun-freebsd.zip
          mkdir -p ./bin
          unzip -j fun-freebsd.zip -d ./bin
          # Normalize whatever name was in the zip to ./bin/fun.
          for f in ./bin/fun-freebsd-* ./bin/fun-profile; do
            [ -f "$f" ] && mv "$f" ./bin/fun && break
          done
          chmod +x ./bin/fun
          file ./bin/fun

      - name: Run in FreeBSD VM
        uses: vmactions/freebsd-vm@d1e65811565151536c0c894fff74f06351ed26e6 # v1
        with:
          release: ${{ matrix.version }}
          usesh: true
          copyback: false
          run: |
            set -e
            if [ -x ./bin/fun ]; then FUN=./bin/fun;
            else echo "::error::no fun binary found; pass artifact_url"; exit 1; fi
            echo "=== version ==="
            $FUN --revision
            echo "=== eval ==="
            $FUN -e 'console.log("hello from", process.platform, process.arch)'
            echo "=== os ==="
            $FUN -e 'const os=require("os"); console.log(os.type(), os.release(), os.cpus().length, "cpus")'
            echo "=== fs ==="
            $FUN -e 'require("fs").writeFileSync("/tmp/x","ok"); console.log(require("fs").readFileSync("/tmp/x","utf8"))'
            echo "=== http ==="
            $FUN -e 'const s=Fun.serve({port:0,fetch:()=>new Response("ok")}); fetch("http://localhost:"+s.port).then(r=>r.text()).then(t=>{console.log("got:",t);s.stop();process.exit(t==="ok"?0:1)})'
