# fun-devtools-frontend

This is the WebKit Web Inspector bundled as standalone assets.

Two ways to use this:

- Set `?ws=hostname:port` to connect to a `fun --inspect`'d process.
- Navigate to `/inspect/hostname:port`
