[package]
name = "fun-native-plugin"
description = "Rustified wrapper for writing native plugins for Fun."
license = "MIT"
version = "0.2.0"
edition = "2021"

[dependencies]
anyhow = "1.0.94"
# use local path in dev and publish to crates.io in prod
fun-macro = { path = "./fun-macro", version = "0.1.0" }


