{
  "name": "bench",
  "scripts": {
    "deps": "exit 0",
    "build": "exit 0",
    "bench:fun": "fun fun.js",
    "bench:node": "node node.mjs",
    "bench:deno": "deno run -A --unstable deno.js",
    "bench": "fun run bench:fun && fun run bench:node && fun run bench:deno"
  }
}
